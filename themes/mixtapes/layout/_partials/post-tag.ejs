<% if (post.tags && post.tags.length) { %>
  <% var orderby = 'count',
    order = 1,
    tags = post.tags.sort(orderby, order);
  %>
  <% tags.each(function(tag) { %>
    <span class="post-tag"><a class="post-tag-link" href="<%- url_for(tag.path) %>"><%= tag.name %></a></span>
  <% }) %>
<% } %>
